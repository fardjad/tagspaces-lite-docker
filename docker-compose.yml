services:
  tagspaces-lite-web:
    # image: fardjad/tagspaces-lite-web:latest
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: bridge
    container_name: tagspaces-lite-web
    ports:
      - "8080:8080"